/**
 * PostCSS Configuration
 * 
 * Configures PostCSS plugins for processing CSS.
 * Uses Tailwind CSS and Autoprefixer for vendor prefixes.
 */
export default {
    plugins: {
        tailwindcss: {},
        autoprefixer: {},
    },
}
